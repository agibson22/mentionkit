{
  "name": "mentionkit-react",
  "version": "0.2.1",
  "publishConfig": {
    "access": "public"
  },
  "description": "React helpers/components for mentionkit (@mentions pills).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepack": "npm run build",
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "test": "echo \"TODO\" && exit 1"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
